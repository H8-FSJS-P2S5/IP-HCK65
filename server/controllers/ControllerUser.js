const express = require('express');
const SpotifyWebApi = require('spotify-web-api-node');

class ControllerUser {
    static async getInfoCurrentUser() {
        try {
            let data = await User.find
        } catch (error) {
            console.log(error);
        }
    }
}

module.exports = ControllerUser